import { DirectiveCardDirective } from './directive-card.directive';

describe('DirectiveCardDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveCardDirective();
    expect(directive).toBeTruthy();
  });
});
